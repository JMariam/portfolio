import React from "react";

interface ProjectsProps {
  title: string;
  img: string;
  text: string;
}

export default function Projects({ title, img, text }: ProjectsProps) {
  return(
    <div className="">

    </div>
  )
}
